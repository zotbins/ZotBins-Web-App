


// $(document).ready(function(){
	// if(sessionStorage.getItem("email") !== null || sessionStorage.getItem("tippershost") !== null){
		// console.log(sessionStorage)
		// var email = sessionStorage.getItem("email")
		// var tippershost = sessionStorage.getItem("tippershost")
		// $.ajax({
			// url: $SCRIPT_ROOT + "/set_session",
			// type: "POST",
			// data: JSON.stringify({"email": email, 
									// "tippershost": tippershost}),
			// contentType:"application/json; charset=utf-8",
			// dataType:"json",
			// success: function(data){
				// $("#header").prepend("<h1 id='welcome'>Welcome, " + data["email"] + "</h1>");
				// $("#navbar-name").prepend(data["email"])
			// }
		// });
	// }
	// else{
		// window.location.replace("/Tportal");
	// }
// });